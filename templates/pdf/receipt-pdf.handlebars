<!DOCTYPE html>
<html lang="el">
<head>
    <meta charset="UTF-8">
    <title>Απόδειξη {{document.documentNumber}}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'DejaVu Sans', 'Helvetica', Arial, sans-serif;
            font-size: 11pt;
            line-height: 1.5;
            color: #000;
        }
        
        .receipt {
            width: 80mm; /* Thermal printer width */
            margin: 0 auto;
            padding: 5mm;
            background: white;
        }
        
        /* For A4 printing */
        .receipt-a4 {
            width: 210mm;
            min-height: 297mm;
            padding: 20mm;
            margin: 0 auto;
            background: white;
        }
        
        .receipt-a4 .receipt-content {
            width: 100mm;
            margin: 0 auto;
            border: 1px dashed #ccc;
            padding: 10mm;
        }
        
        /* Header */
        .header {
            text-align: center;
            padding-bottom: 10px;
            border-bottom: 1px solid #000;
            margin-bottom: 10px;
        }
        
        .company-name {
            font-size: 14pt;
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        .company-info {
            font-size: 9pt;
            color: #333;
            line-height: 1.3;
        }
        
        /* Receipt title */
        .receipt-title {
            text-align: center;
            font-size: 12pt;
            font-weight: bold;
            margin: 10px 0;
            padding: 5px;
            background-color: #f0f0f0;
            border: 1px solid #000;
        }
        
        /* Receipt details */
        .receipt-details {
            margin: 10px 0;
            font-size: 10pt;
        }
        
        .detail-row {
            display: flex;
            justify-content: space-between;
            margin: 3px 0;
        }
        
        .detail-label {
            font-weight: bold;
        }
        
        .detail-value {
            text-align: right;
        }
        
        /* Items section */
        .items-section {
            margin: 15px 0;
            border-top: 1px dashed #666;
            border-bottom: 1px dashed #666;
            padding: 10px 0;
        }
        
        .item-row {
            margin: 8px 0;
            font-size: 10pt;
        }
        
        .item-description {
            font-weight: 500;
        }
        
        .item-details {
            display: flex;
            justify-content: space-between;
            font-size: 9pt;
            color: #666;
            margin-top: 2px;
        }
        
        .item-quantity {
            flex: 1;
        }
        
        .item-price {
            flex: 1;
            text-align: center;
        }
        
        .item-total {
            flex: 1;
            text-align: right;
            font-weight: bold;
            color: #000;
        }
        
        /* Totals */
        .totals-section {
            margin: 15px 0;
            padding-top: 10px;
            border-top: 1px solid #000;
        }
        
        .total-row {
            display: flex;
            justify-content: space-between;
            margin: 5px 0;
            font-size: 10pt;
        }
        
        .total-row.subtotal {
            font-size: 9pt;
            color: #666;
        }
        
        .total-row.grand-total {
            font-size: 14pt;
            font-weight: bold;
            padding: 8px 0;
            border-top: 2px solid #000;
            border-bottom: 2px solid #000;
            margin: 10px 0;
        }
        
        /* Payment info */
        .payment-section {
            margin: 15px 0;
            padding: 10px;
            background-color: #f8f8f8;
            border: 1px solid #ddd;
            font-size: 10pt;
        }
        
        .payment-method {
            font-weight: bold;
            margin-bottom: 5px;
        }
        
        /* Footer */
        .footer {
            margin-top: 20px;
            padding-top: 10px;
            border-top: 1px dashed #666;
            text-align: center;
            font-size: 9pt;
            color: #666;
        }
        
        .barcode {
            text-align: center;
            margin: 15px 0;
            font-family: 'Libre Barcode 128', monospace;
            font-size: 36pt;
        }
        
        .qr-code {
            text-align: center;
            margin: 15px 0;
        }
        
        .qr-code img {
            width: 60mm;
            height: 60mm;
        }
        
        /* Tax summary */
        .tax-summary {
            margin: 10px 0;
            padding: 8px;
            background-color: #f0f0f0;
            font-size: 9pt;
        }
        
        .tax-row {
            display: flex;
            justify-content: space-between;
            margin: 3px 0;
        }
        
        /* Messages */
        .thank-you {
            text-align: center;
            font-size: 11pt;
            font-weight: bold;
            margin: 15px 0;
            padding: 10px;
            border: 1px solid #000;
        }
        
        .info-message {
            text-align: center;
            font-size: 9pt;
            color: #666;
            margin: 10px 0;
            font-style: italic;
        }
        
        /* Print specific */
        @media print {
            .receipt {
                page-break-after: always;
            }
            
            .no-print {
                display: none;
            }
        }
        
        /* Stamp/Watermark */
        .paid-stamp {
            position: relative;
            text-align: center;
            margin: 20px 0;
        }
        
        .paid-stamp::after {
            content: "ΕΞΟΦΛΗΘΗ";
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-15deg);
            font-size: 24pt;
            font-weight: bold;
            color: rgba(0, 128, 0, 0.3);
            border: 3px solid rgba(0, 128, 0, 0.3);
            padding: 10px 20px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    
    <div class="{{#if isA4}}receipt-a4{{else}}receipt{{/if}}">
        {{#if isA4}}<div class="receipt-content">{{/if}}
        
        <!-- Header -->
        <div class="header">
            <div class="company-name">{{company.name}}</div>
            <div class="company-info">
                {{company.address}}<br>
                {{company.city}}, {{company.postalCode}}<br>
                ΑΦΜ: {{company.vatNumber}}<br>
                ΔΟΥ: {{company.taxOffice}}<br>
                Τηλ: {{company.phone}}
            </div>
        </div>
        
        <!-- Receipt Title -->
        <div class="receipt-title">
            ΑΠΟΔΕΙΞΗ ΛΙΑΝΙΚΗΣ ΠΩΛΗΣΗΣ
        </div>
        
        <!-- Receipt Details -->
        <div class="receipt-details">
            <div class="detail-row">
                <span class="detail-label">ΑΡ. ΑΠΟΔΕΙΞΗΣ:</span>
                <span class="detail-value">{{document.documentNumber}}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">ΗΜΕΡΟΜΗΝΙΑ:</span>
                <span class="detail-value">{{formatDate document.date}}</span>
            </div>
            <div class="detail-row">
                <span class="detail-label">ΩΡΑ:</span>
                <span class="detail-value">{{formatTime document.createdAt}}</span>
            </div>
            {{#if customer.name}}
            <div class="detail-row">
                <span class="detail-label">ΠΕΛΑΤΗΣ:</span>
                <span class="detail-value">{{customer.name}}</span>
            </div>
            {{/if}}
            {{#if customer.vatNumber}}
            <div class="detail-row">
                <span class="detail-label">ΑΦΜ ΠΕΛΑΤΗ:</span>
                <span class="detail-value">{{customer.vatNumber}}</span>
            </div>
            {{/if}}
            {{#if cashier}}
            <div class="detail-row">
                <span class="detail-label">ΤΑΜΙΑΣ:</span>
                <span class="detail-value">{{cashier}}</span>
            </div>
            {{/if}}
        </div>
        
        <!-- Items -->
        <div class="items-section">
            {{#each items}}
            <div class="item-row">
                <div class="item-description">
                    {{this.description}}
                    {{#if this.productCode}}({{this.productCode}}){{/if}}
                </div>
                <div class="item-details">
                    <span class="item-quantity">{{this.quantity}} {{this.unit}} x {{formatCurrency this.unitPrice}}</span>
                    <span class="item-total">{{formatCurrency this.totalAmount}}</span>
                </div>
                {{#if this.discountPercentage}}
                <div style="font-size: 8pt; color: #666; margin-left: 10px;">
                    Έκπτωση {{this.discountPercentage}}%: -{{formatCurrency this.discountAmount}}
                </div>
                {{/if}}
            </div>
            {{/each}}
        </div>
        
        <!-- Tax Summary -->
        <div class="tax-summary">
            <div class="tax-row">
                <span>ΥΠΟΣΥΝΟΛΟ:</span>
                <span>{{formatCurrency totals.subtotal}}</span>
            </div>
            {{#if totals.totalDiscount}}
            <div class="tax-row">
                <span>ΕΚΠΤΩΣΗ:</span>
                <span>-{{formatCurrency totals.totalDiscount}}</span>
            </div>
            {{/if}}
            <div class="tax-row">
                <span>ΚΑΘΑΡΗ ΑΞΙΑ:</span>
                <span>{{formatCurrency totals.netAmount}}</span>
            </div>
            {{#each totals.vatBreakdown}}
            <div class="tax-row">
                <span>ΦΠΑ {{this.rate}}%:</span>
                <span>{{formatCurrency this.amount}}</span>
            </div>
            {{/each}}
        </div>
        
        <!-- Grand Total -->
        <div class="totals-section">
            <div class="total-row grand-total">
                <span>ΣΥΝΟΛΟ:</span>
                <span>{{formatCurrency totals.totalAmount}}</span>
            </div>
        </div>
        
        <!-- Payment Section -->
        <div class="payment-section">
            <div class="payment-method">
                ΤΡΟΠΟΣ ΠΛΗΡΩΜΗΣ: {{paymentMethodLabel}}
            </div>
            {{#if isCash}}
            <div class="detail-row" style="margin-top: 5px;">
                <span>ΜΕΤΡΗΤΑ:</span>
                <span>{{formatCurrency payment.cashGiven}}</span>
            </div>
            <div class="detail-row">
                <span>ΡΕΣΤΑ:</span>
                <span>{{formatCurrency payment.change}}</span>
            </div>
            {{/if}}
            {{#if isCard}}
            <div style="font-size: 9pt; color: #666; margin-top: 5px;">
                Κάρτα: **** {{payment.cardLastFour}}<br>
                {{#if payment.authCode}}Κωδ. Έγκρισης: {{payment.authCode}}{{/if}}
            </div>
            {{/if}}
        </div>
        
        <!-- myDATA Info -->
        {{#if document.mydataMark}}
        <div style="text-align: center; margin: 10px 0; padding: 8px; background-color: #e8f5e9; font-size: 9pt;">
            <strong>ΜΑΡΚ myDATA:</strong><br>
            {{document.mydataMark}}
        </div>
        {{/if}}
        
        <!-- QR Code -->
        {{#if document.mydataQr}}
        <div class="qr-code">
            <img src="{{document.mydataQr}}" alt="QR Code">
            <div style="font-size: 8pt; color: #666;">Σκανάρετε για επαλήθευση</div>
        </div>
        {{/if}}
        
        <!-- Barcode -->
        {{#if document.barcode}}
        <div class="barcode">
            *{{document.documentNumber}}*
        </div>
        {{/if}}
        
        <!-- Thank You Message -->
        <div class="thank-you">
            ΕΥΧΑΡΙΣΤΟΥΜΕ ΓΙΑ ΤΗΝ ΠΡΟΤΙΜΗΣΗ ΣΑΣ!
        </div>
        
        <!-- Info Messages -->
        {{#if showReturnPolicy}}
        <div class="info-message">
            Επιστροφές εντός 14 ημερών με την απόδειξη
        </div>
        {{/if}}
        
        {{#if showWarranty}}
        <div class="info-message">
            Εγγύηση σύμφωνα με τους όρους του κατασκευαστή
        </div>
        {{/if}}
        
        <!-- Footer -->
        <div class="footer">
            {{#if company.website}}
            {{company.website}}<br>
            {{/if}}
            {{#if company.email}}
            {{company.email}}<br>
            {{/if}}
            <div style="margin-top: 10px;">
                Εκτύπωση: {{formatDateTime printDate}}<br>
                {{#if isReprint}}
                <strong>ΑΝΤΙΓΡΑΦΟ</strong>
                {{/if}}
            </div>
        </div>
        
        <!-- Paid Stamp (if applicable) -->
        {{#if isPaid}}
        <div class="paid-stamp" style="height: 60px;"></div>
        {{/if}}
        
        {{#if isA4}}</div>{{/if}}
    </div>
    
</body>
</html>